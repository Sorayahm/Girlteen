'use strict';

goog.provide('Blockly.JavaScript.Scratch');

goog.require('Blockly.JavaScript');

Blockly.JavaScript['move'] = function(block) {
  var number_name = block.getFieldValue('NAME');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['turn_right'] = function(block) {
  var number_name = block.getFieldValue('NAME');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['turn_left'] = function(block) {
  var number_name = block.getFieldValue('NAME');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['go_to'] = function(block) {
  var dropdown_position = block.getFieldValue('POSITION');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['go_to_coordenate'] = function(block) {
  var number_name = block.getFieldValue('NAME');
  var number_name = block.getFieldValue('NAME');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['glide'] = function(block) {
  var number_name = block.getFieldValue('NAME');
  var dropdown_position = block.getFieldValue('POSITION');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['glide_coordenate'] = function(block) {
  var number_name = block.getFieldValue('NAME');
  var number_position_x = block.getFieldValue('POSITION X');
  var number_position_y = block.getFieldValue('POSITION Y');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['degrees'] = function(block) {
  var angle_degrees = block.getFieldValue('DEGREES');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['point_towards'] = function(block) {
  var dropdown_position = block.getFieldValue('POSITION');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['change_x'] = function(block) {
  var number_position = block.getFieldValue('POSITION');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['change_y'] = function(block) {
  var number_position = block.getFieldValue('POSITION');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['set_x'] = function(block) {
  var number_position = block.getFieldValue('POSITION');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['set_y'] = function(block) {
  var number_position = block.getFieldValue('POSITION');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['bounce'] = function(block) {
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['rotate'] = function(block) {
  var dropdown_rotation = block.getFieldValue('ROTATION');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['position_x'] = function(block) {
  var checkbox_name = block.getFieldValue('NAME') == 'TRUE';
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['position_y'] = function(block) {
  var checkbox_name = block.getFieldValue('NAME') == 'TRUE';
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['direction'] = function(block) {
  var checkbox_name = block.getFieldValue('NAME') == 'TRUE';
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['say_sec'] = function(block) {
  var text_text = block.getFieldValue('TEXT');
  var number_name = block.getFieldValue('NAME');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['say'] = function(block) {
  var text_text = block.getFieldValue('TEXT');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['think_sec'] = function(block) {
  var text_text = block.getFieldValue('TEXT');
  var number_name = block.getFieldValue('NAME');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['think'] = function(block) {
  var text_text = block.getFieldValue('TEXT');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['change_size'] = function(block) {
  var number_size = block.getFieldValue('SIZE');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['change_size_percentage'] = function(block) {
  var number_size = block.getFieldValue('SIZE');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['when_flag'] = function(block) {
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['key_pressed'] = function(block) {
  var dropdown_key = block.getFieldValue('KEY');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['sprite_press'] = function(block) {
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['msg_receive'] = function(block) {
  var dropdown_msg = block.getFieldValue('MSG');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['msg_broadcast'] = function(block) {
  var dropdown_msg = block.getFieldValue('MSG');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};